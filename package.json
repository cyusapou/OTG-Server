{
  "name": "onthego-server",
  "version": "1.0.0",
  "type": "module",
  "description": "On The Go — JSON Server API (deployable)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1",
    "json-server": "^1.0.0-beta.12"
  },
  "engines": {
    "node": ">=18"
  }
}
